<?xml version="1.0" encoding="UTF-8"?>
<project name="CatacombSnatchLevelEditor" default="default" basedir=".">
    <description>Builds, tests, and runs the project CatacombSnatchLevelEditor.</description>
    <import file="nbproject/build-impl.xml"/>
    
    <target name="-post-jar">
        <copy todir="${dist.dir}/art">
            <fileset dir="${basedir}/art" />
        </copy>
        <copy todir="${dist.dir}/levels">
            <fileset dir="${basedir}/levels" />
        </copy>
    </target>
    
</project>